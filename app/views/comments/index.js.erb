$("#comments_area").html("<%= j(render 'comments/index', { comments: @comment.rob.comments, rob: @comment.rob }) %>")
$("textarea").val('')
$('body').on('ajax:success', '.delete-comment-button', function(event) {
  $(event.original_target).closest('.single-comment').fadeOut();
})